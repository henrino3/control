<p id="notice"><%= notice %></p>

<h1>Listing Citizens</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone num1</th>
      <th>Phone num2</th>
      <th>Email</th>
      <th>Address</th>
      <th>Nat</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @citizens.each do |citizen| %>
      <tr>
        <td><%= citizen.name %></td>
        <td><%= citizen.phone_num1 %></td>
        <td><%= citizen.phone_num2 %></td>
        <td><%= citizen.email %></td>
        <td><%= citizen.address %></td>
        <td><%= citizen.nat_id %></td>
        <td><%= link_to 'Show', citizen %></td>
        <td><%= link_to 'Edit', edit_citizen_path(citizen) %></td>
        <td><%= link_to 'Destroy', citizen, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Citizen', new_citizen_path %>
